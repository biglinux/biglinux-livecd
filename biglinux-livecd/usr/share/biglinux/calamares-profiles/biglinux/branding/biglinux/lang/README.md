# Branding translations

The stable hybrid welcome page keeps its source strings in English and uses
`../i18n.js` for the BigLinux-specific Portuguese translation. Standard module,
sidebar-model and navigation labels continue to use Calamares translations.

A compiled branding catalog can replace this small dictionary later when the
project adds more languages to the custom copy.
